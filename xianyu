#!/usr/bin/env python3
"""Compatibility wrapper for the skill-bundled Xianyu CLI."""

from pathlib import Path
import runpy
import sys


ROOT = Path(__file__).resolve().parent
SCRIPTS_DIR = ROOT / "skills" / "xianyu-cli-operations" / "scripts"

sys.path.insert(0, str(SCRIPTS_DIR))
runpy.run_path(str(SCRIPTS_DIR / "xianyu"), run_name="__main__")
